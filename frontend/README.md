# Frontend

The web client is a Next.js App Router application using TypeScript and Tailwind CSS. Phase 1 provides the responsive visual shell and deliberately has no form submission, upload, authentication, or API consumption logic.

```bash
pnpm install
pnpm dev
pnpm build
pnpm check
```
